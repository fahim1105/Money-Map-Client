import React from "react";
import Hero from "../Hero/Hero";

const Home = () => {
    return (
        <main className="w-full min-h-screen overflow-hidden">
            <Hero />
        </main>
    );
};

export default Home;
